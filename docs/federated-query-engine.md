# Federated Query Engine

This page is retained for backward-compatible linking.

The current engine docs are split across:

- `docs/architecture/federated-query-engine.md`
- `docs/features/federation.md`
- `docs/architecture/overview.md`

## Current Positioning

- The built-in federated planner and executor is the primary structured data engine.
- Semantic and SQL runtime workloads can route through federation in the same runtime execution path.
- Federation is part of the runtime host and embedded runtime story.
